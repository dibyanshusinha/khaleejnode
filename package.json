{
  "name": "khaleejnode",
  "version": "1.0.0",
  "private": true,
  "description": "KhaleejNode — Tauri desktop shell (Rust Shield + Python Brain + web UI).",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "info": "tauri info"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
