# KhaleejNode runtime dependencies.
# Both are optional-at-worst: the app degrades gracefully if unavailable, but
# these are the production defaults.

pydantic>=2.0        # typed UAE customs manifest contract (The Brain)
cryptography>=41.0   # Ed25519 asymmetric signing for offline credit tokens
pymupdf>=1.24        # render shipping PDFs to page images for vision extraction
pyspellchecker>=0.8  # offline spell check for commodity descriptions (typo flags)
keyring>=24.0.0      # secure local OS credential storage for balance integrity

# Benchmark harness only (benchmark/) — not needed by the app/sidecar at runtime:
Pillow>=10.0         # image degradation for the accuracy stress-test
numpy>=1.24          # noise / warp transforms in the degradation harness

# The real extraction engine is a LOCAL Llama 3.2 Vision model served by Ollama
# (https://ollama.com) at localhost:11434 — install separately and run:
#     ollama pull llama3.2-vision
# The vision path is reached over loopback only; the app stays 100% offline.
# If Ollama/the model is absent, the Brain falls back to the deterministic mock.
