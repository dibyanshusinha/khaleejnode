{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability: lets the main window call the app's own commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
